/** @hidden */
class SnapOffset {

    guidePosition: number;
    objectPosition: number;

    constructor(guidePosition: number, objectPosition: number) {
        this.guidePosition = guidePosition;
        this.objectPosition = objectPosition;
    }

}